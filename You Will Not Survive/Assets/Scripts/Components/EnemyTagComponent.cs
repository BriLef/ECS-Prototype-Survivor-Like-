using Unity.Entities;

namespace Components
{
    public struct EnemyTag : IComponentData
    {
        // Tag component to identify enemy entities
        // No data needed, just serves as a marker
    }
} 